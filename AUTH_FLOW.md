# Тіркелу және Кіру Функциялары - Қалай Жұмыс Істейді

## 📝 Тіркелу (Register)

### 1. Email/Password арқылы тіркелу

**Қадамдар:**
1. Пайдаланушы форманы толтырады:
   - Пайдаланушы аты (username)
   - Электрондық пошта (email)
   - Құпия сөз (password)
   - Құпия сөзді растау (confirmPassword)

2. Валидация:
   - Құпия сөздер сәйкес келуі керек
   - Құпия сөз кемінде 6 таңбадан тұруы керек
   - Email дұрыс форматта болуы керек
   - Барлық өрістер толтырылуы керек

3. Firebase Authentication:
   - `createUserWithEmailAndPassword()` - пайдаланушыны Firebase-те тіркеу
   - `updateProfile()` - пайдаланушы профилінде username-ді сақтау

4. Деректерді сақтау:
   - **localStorage** - пайдаланушы деректерін браузерде сақтау
   - **Firestore** - пайдаланушыны іздеу үшін сақтау
   - **Backend API** - пайдаланушыны backend-те синхрондау (опционал)

5. Бағыттау:
   - Тіркелу сәтті болғаннан кейін басты бетке (`/`) бағыттау

### 2. Google Sign-In арқылы тіркелу

**Қадамдар:**
1. Пайдаланушы "Google арқылы тіркелу" батырмасын басады
2. Google popup терезесі ашылады
3. Пайдаланушы Google аккаунтымен кіреді
4. Firebase Authentication:
   - `signInWithPopup()` - Google арқылы кіру
   - Пайдаланушы деректерін Firebase-тен алу (UID, email, displayName, photoURL)
5. Деректерді сақтау (Email/Password сияқты)
6. Бағыттау басты бетке

---

## 🔐 Кіру (Login)

### 1. Email/Password арқылы кіру

**Қадамдар:**
1. Пайдаланушы форманы толтырады:
   - Ник немесе электрондық пошта (emailOrUsername)
   - Құпия сөз (password)

2. Валидация:
   - Барлық өрістер толтырылуы керек

3. Email немесе Username анықтау:
   - Егер `@` белгісі бар болса → Email
   - Егер `@` белгісі жоқ болса → Username

4. Username болса:
   - Алдымен Backend API-ға сұрау жіберу
   - Егер сәтті болса → Backend деректерін пайдалану
   - Егер сәтсіз болса → Firebase-ке username-ді email ретінде жіберу

5. Firebase Authentication:
   - `signInWithEmailAndPassword()` - Firebase-те кіру

6. Деректерді сақтау:
   - **localStorage** - пайдаланушы деректерін сақтау
   - **Firestore** - пайдаланушыны іздеу үшін сақтау
   - **Backend API** - синхрондау (опционал)

7. Бағыттау басты бетке

### 2. Google Sign-In арқылы кіру

**Қадамдар:**
1. Пайдаланушы "Google арқылы кіру" батырмасын басады
2. Google popup терезесі ашылады
3. Пайдаланушы Google аккаунтымен кіреді
4. Firebase Authentication (Тіркелу сияқты)
5. Деректерді сақтау
6. Бағыттау басты бетке

---

## 🔄 Жұмыс Істеу Тәртібі

### Тіркелу кезінде:
```
1. Форма валидациясы
   ↓
2. Firebase Authentication (тіркелу)
   ↓
3. Firebase Profile жаңарту (username)
   ↓
4. localStorage-ға сақтау
   ↓
5. Firestore-ға сақтау (іздеу үшін)
   ↓
6. Backend API-ға синхрондау (опционал)
   ↓
7. Басты бетке бағыттау
```

### Кіру кезінде:
```
1. Форма валидациясы
   ↓
2. Email/Username анықтау
   ↓
3. Username болса → Backend API тексеру
   ↓
4. Firebase Authentication (кіру)
   ↓
5. localStorage-ға сақтау
   ↓
6. Firestore-ға сақтау
   ↓
7. Backend API-ға синхрондау (опционал)
   ↓
8. Басты бетке бағыттау
```

---

## 📦 Деректерді Сақтау

### 1. localStorage
- **Мақсаты**: Пайдаланушы сессиясын сақтау
- **Деректер**: `{ id, username, email, avatar }`
- **Қолданылуы**: Барлық беттерде пайдаланушыны анықтау

### 2. Firestore
- **Мақсаты**: Пайдаланушыларды іздеу
- **Деректер**: `{ username, email, avatar, updatedAt }`
- **Қолданылуы**: Чат бетінде достарды іздеу

### 3. Backend API
- **Мақсаты**: Пайдаланушы деректерін синхрондау
- **Деректер**: `{ id, username, email, avatar }`
- **Қолданылуы**: Backend деректерімен жұмыс істеу

---

## ⚠️ Қателерді Өңдеу

### Тіркелу қателері:
- `auth/email-already-in-use` - Email бойынша тіркелгі бар
- `auth/invalid-email` - Email дұрыс емес
- `auth/weak-password` - Құпия сөз тым әлсіз
- `auth/operation-not-allowed` - Email/Password аутентификациясы қосылмаған

### Кіру қателері:
- `auth/user-not-found` - Пайдаланушы табылмады
- `auth/wrong-password` - Құпия сөз дұрыс емес
- `auth/invalid-email` - Email дұрыс емес
- `auth/invalid-credential` - Email немесе құпия сөз дұрыс емес

---

## 🔧 Техникалық Детальдар

### Firebase Authentication
- **Қызмет**: Google Firebase Authentication
- **Функциялар**: 
  - `createUserWithEmailAndPassword()` - тіркелу
  - `signInWithEmailAndPassword()` - кіру
  - `signInWithPopup()` - Google Sign-In
  - `updateProfile()` - профиль жаңарту

### Backend API
- **Endpoint**: `http://127.0.0.1:3000/api`
- **Функциялар**:
  - `POST /api/auth/register` - тіркелу
  - `POST /api/auth/login` - кіру

### Firestore
- **Collection**: `users`
- **Document ID**: Firebase UID
- **Fields**: `username`, `email`, `avatar`, `updatedAt`

---

## ✅ Тестілеу

### Тіркелуді тестілеу:
1. `/register` бетіне барыңыз
2. Форманы толтырыңыз
3. "Тіркелу" батырмасын басыңыз
4. Басты бетке бағытталуы керек

### Кіруді тестілеу:
1. `/login` бетіне барыңыз
2. Email/Password енгізіңіз
3. "Кіру" батырмасын басыңыз
4. Басты бетке бағытталуы керек

### Google Sign-In тестілеу:
1. "Google арқылы кіру/тіркелу" батырмасын басыңыз
2. Google аккаунтымен кіріңіз
3. Басты бетке бағытталуы керек

---

## 🔧 Қателерді Шешу (Troubleshooting)

### "A valid domain name is required" қатесі

Бұл қате көбінесе Google Sign-In-ді настройкалау кезінде көрінеді. Келесі қадамдарды орындаңыз:

#### 1. Firebase Console-да Authorized Domains қосу

1. [Firebase Console](https://console.firebase.google.com/) → **Authentication** → **Settings** табына барыңыз
2. **Authorized domains** бөлімінде **Add domain** батырмасын басыңыз
3. Келесі домендерді бір-бірден қосыңыз:
   ```
   localhost
   127.0.0.1
   ```
   **Маңызды:** Тек домен атауын енгізіңіз (протокол `http://` және порт `:5173` емес)
4. Әр доменді қосқаннан кейін **Add** батырмасын басыңыз

#### 2. Google Cloud Console-да OAuth настройкасы

Егер қате Google Cloud Console-да көрінсе:

1. [Google Cloud Console](https://console.cloud.google.com/) сайтына барыңыз
2. Проектіңізді таңдаңыз (Firebase проектіңізбен бірдей - **kazakh-hub**)
3. **APIs & Services** → **Credentials** табына барыңыз
4. **OAuth 2.0 Client IDs** бөлімінде клиентті табыңыз (немесе жаңасын құрыңыз)
5. Клиентті ашып, келесі настройкаларды жасаңыз:

   **Authorized JavaScript origins:**
   ```
   http://localhost:5173
   http://127.0.0.1:5173
   ```

   **Authorized redirect URIs:**
   ```
   http://localhost:5173
   https://kazakh-hub.firebaseapp.com/__/auth/handler
   ```

6. **Save** батырмасын басыңыз

#### 3. OAuth Consent Screen настройкасы

Егер OAuth consent screen настройкаланбаған болса:

1. Google Cloud Console-да **APIs & Services** → **OAuth consent screen** табына барыңыз
2. **User Type** таңдаңыз (көбінесе "External" - сыртқы пайдаланушылар үшін)
3. **App information** бөлімінде:
   - **App name**: Kazakh Hub (немесе өз атауыңыз)
   - **User support email**: өз email-іңізді енгізіңіз
   - **Developer contact information**: өз email-іңізді енгізіңіз
4. **Scopes** бөлімінде **Add or Remove Scopes** батырмасын басып, келесі scope-тарды қосыңыз:
   - `.../auth/userinfo.email`
   - `.../auth/userinfo.profile`
   - `openid`
5. **Test users** бөлімінде (егер External таңдасаңыз) өз email-іңізді қосыңыз
6. **Save and Continue** батырмасын басыңыз

#### 4. Тестілеу

Настройкаларды жасағаннан кейін:
1. Браузерді жабып, қайта ашыңыз
2. `/login` немесе `/register` бетіне барыңыз
3. "Google арқылы кіру" батырмасын басып тексеріңіз

**Ескерту:** Настройкалардың жүктелуіне 5-10 минут уақыт кетуі мүмкін.

