# "A valid domain name is required" қатесін шешу

## ⚡ Жылдам шешім

### Firebase Console-да:

1. [Firebase Console](https://console.firebase.google.com/) → **Authentication** → **Settings**
2. **Authorized domains** бөлімінде **Add domain** батырмасын басыңыз
3. Төмендегі мәндерді дәл осылай енгізіңіз (протокол және портсыз):

```
localhost
```

4. **Add** батырмасын басыңыз
5. Тағы бір рет **Add domain** батырмасын басып, мынаны енгізіңіз:

```
127.0.0.1
```

6. **Add** батырмасын басыңыз

### Google Cloud Console-да (егер қате осы жерде көрінсе):

1. [Google Cloud Console](https://console.cloud.google.com/) → **APIs & Services** → **Credentials**
2. **OAuth 2.0 Client IDs** бөлімінде клиентті табыңыз
3. Клиентті ашып, төмендегі мәндерді енгізіңіз:

**Authorized JavaScript origins** (әрбірін бөлек қосу):
```
http://localhost:5173
http://127.0.0.1:5173
```

**Authorized redirect URIs** (әрбірін бөлек қосу):
```
http://localhost:5173
https://kazakh-hub.firebaseapp.com/__/auth/handler
```

4. **Save** батырмасын басыңыз

## 📝 Ескертулер

- **Firebase Authorized domains** бөлімінде тек домен атауын енгізіңіз (мысалы: `localhost`)
- **Google Cloud OAuth** бөлімінде толық URL енгізіңіз (протокол мен портпен)
- Настройкалардың жүктелуіне 5-10 минут уақыт кетуі мүмкін
- Настройкаларды жасағаннан кейін браузерді қайта бастаңыз

## ✅ Тестілеу

1. Браузерді жабып, қайта ашыңыз
2. `http://localhost:5173/login` бетіне барыңыз
3. "Google арқылы кіру" батырмасын басыңыз
4. Егер Google терезесі ашылатын болса, настройкалар дұрыс жасалған

## 🆘 Егер әлі де қате көрінсе

1. Firebase Console-да **Authentication** → **Sign-in method** табына барыңыз
2. **Google** әдісін ашып, **Project support email** енгізілгенін тексеріңіз
3. **Save** батырмасын басыңыз
4. 5-10 минут күтіп, қайта тексеріңіз

